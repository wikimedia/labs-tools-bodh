import { Typography } from '@material-ui/core'

function Login() {
    return (
        <>
            <Typography style={{ marginLeft: "5%", marginTop: "5%"}}>
                Please login first to use the tool.
            </Typography>
        </>
    );
}

export default Login;